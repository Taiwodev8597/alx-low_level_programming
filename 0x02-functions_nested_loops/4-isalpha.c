#include "main.h"
/**
 *isalpha - program to that checks for alphabetic letters
 *
 *@c: Character to be checked
 *Return: 1 if alphabet and 0 if otherwise
 */
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') ||
(c >= 'A' && C<= 'Z')
return (1);
else
return (0);
}
